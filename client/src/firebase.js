import { initializeApp } from 'firebase/app';

const firebaseConfig = {
    apiKey: "",
    authDomain: "cominghome-findpet.firebaseapp.com"
};

const app = initializeApp(firebaseConfig);
export default app;
  